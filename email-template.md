# Ghost 鑷畾涔?Newsletter 妯℃澘娓叉煋杩囩▼鍒嗘瀽

## 鐩綍

1. [鏁翠綋鏋舵瀯](#鏁翠綋鏋舵瀯)
2. [鍚庡彴 Email Designer 涓庝富棰樻ā鏉垮叡浜増寮廬(#鍚庡彴-email-designer-涓庝富棰樻ā鏉垮叡浜増寮?
3. [涓€у寲鍙橀噺缁戝畾鍒颁細鍛樺瓧娈礭(#涓€у寲鍙橀噺缁戝畾鍒颁細鍛樺瓧娈?
4. [鎵归噺鍙戦€佷笌璺ㄩ偖绠卞鎴风鍏煎](#鎵归噺鍙戦€佷笌璺ㄩ偖绠卞鎴风鍏煎)
5. [澶氳瑷€鍏滃簳鏈哄埗](#澶氳瑷€鍏滃簳鏈哄埗)
6. [鍏抽敭鏂囦欢绱㈠紩](#鍏抽敭鏂囦欢绱㈠紩)

---

## 鏁翠綋鏋舵瀯

### 涓夊眰鏈嶅姟鏋舵瀯

Ghost 鐨?newsletter 閭欢娓叉煋绯荤粺閲囩敤涓夊眰鏈嶅姟鏋舵瀯锛?
```
鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?                   BatchSendingService                       鈹?鈹? (鎵归噺璋冨害锛氬垎鎵广€佸苟鍙戞帶鍒躲€侀噸璇曟満鍒躲€佸煙鍚嶉鐑?              鈹?鈹? ghost/core/core/server/services/email-service/              鈹?鈹? batch-sending-service.js                                    鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?                              鈹?                              鈻?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?                     SendingService                          鈹?鈹? (鍙戦€佸崗璋冿細缂撳瓨娓叉煋缁撴灉銆佹瀯寤烘敹浠朵汉鍒楄〃銆佽皟鐢?Provider)     鈹?鈹? ghost/core/core/server/services/email-service/              鈹?鈹? sending-service.js                                          鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?                              鈹?                              鈻?鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?                     EmailRenderer                           鈹?鈹? (鏍稿績娓叉煋锛氭ā鏉胯В鏋愩€佸彉閲忔浛鎹€丆SS 鍐呰仈銆佸鎴风鍏煎澶勭悊)     鈹?鈹? ghost/core/core/server/services/email-service/              鈹?鈹? email-renderer.js                                           鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?```

### 渚濊禆娉ㄥ叆鍒濆鍖?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-service-wrapper.js:13-110`

```javascript
init() {
    // i18n 鍒濆鍖?    const i18nLanguage = settingsCache.get('locale') || 'en';  // email-service-wrapper.js:62
    const i18n = i18nLib(i18nLanguage, 'ghost');              // email-service-wrapper.js:63

    // 鐩戝惉 locale 鍙樺寲
    events.on('settings.locale.edited', (model) => {
        i18n.changeLanguage(model.get('value'));             // email-service-wrapper.js:69
    });

    // EmailRenderer 鍒濆鍖栵紙鍖呭惈 i18n 娉ㄥ叆锛?    const emailRenderer = new EmailRenderer({
        settingsCache,
        settingsHelpers,
        // ...
        t: i18n.t,                                           // email-service-wrapper.js:94
        dir: i18n.dir.bind(i18n)                              // email-service-wrapper.js:95
    });

    // SendingService 鍒濆鍖?    const sendingService = new SendingService({
        emailProvider: mailgunEmailProvider,                   // email-service-wrapper.js:105
        emailRenderer,
        emailAddressService
    });

    // BatchSendingService 鍒濆鍖?    const batchSendingService = new BatchSendingService({
        emailRenderer,
        sendingService,
        jobsService,
        emailSegmenter,
        domainWarmingService,
        models,
        db,
        sentry
    });
}
```

### 娓叉煋娴佺▼鎬昏

| 姝ラ | 鍑芥暟 | 鍔熻兘 | 鏂囦欢 | 绮剧‘琛屽彿 |
|------|------|------|------|----------|
| 1. 瑙﹀彂鍙戦€?| `emailJob({emailId})` | 鐘舵€侀攣 + 璋冪敤 sendEmail | `batch-sending-service.js` | 133-190 |
| 2. 鍒涘缓鎵规 | `createBatches({email, newsletter, post})` | 鍒嗘銆佸垎鎵广€佸煙鍚嶉鐑?| `batch-sending-service.js` | 237-340 |
| 3. 娓叉煋閭欢浣?| `renderBody(post, newsletter, segment, options)` | 瀹屾暣娓叉煋娴佹按绾?| `email-renderer.js` | 393-585 |
| 4. 鏋勫缓鏀朵欢浜?| `buildRecipients(members, replacementDefinitions)` | 浼氬憳瀛楁缁戝畾鍒板彉閲?| `sending-service.js` | 162-182 |
| 5. 鍙戦€?| `send({post, newsletter, segment, members, emailId}, options)` | 缂撳瓨 + 璋冪敤 Provider | `sending-service.js` | 110-154 |

---

## 鍚庡彴 Email Designer 涓庝富棰樻ā鏉垮叡浜増寮?
### 鏍稿績璁捐锛氳涔夊€煎瓨鍌?+ 鐙珛瑙ｆ瀽

**鍏抽敭鍙戠幇**锛氬墠鍚庣涓嶅叡浜绠楀嚱鏁帮紝鑰屾槸鍚勮嚜鐙珛瀹炵幇鐩稿悓鐨勮В鏋愰€昏緫銆?
#### 鏁版嵁娴?
```
鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?                   Newsletters 琛紙鏁版嵁搴撳瓨鍌ㄥ眰锛?                       鈹?鈹? background_color: 'light'  (榛樿鍊硷紝璇箟鍊硷紝闈?HEX)                     鈹?鈹? header_background_color: 'transparent'  (璇箟鍊?                        鈹?鈹? button_color: 'accent'  (璇箟鍊?                                        鈹?鈹? link_color: 'accent'  (璇箟鍊?                                          鈹?鈹? title_font_weight: 'bold'  (鏋氫妇鍊?                                     鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?                                    鈹?            鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹尖攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?            鈻?                      鈻?                      鈻?    鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?     鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?    鈹? 绠＄悊绔瑙?   鈹?     鈹?  閭欢娓叉煋绔?     鈹?   鈹?  HEX 棰滆壊鍦烘櫙   鈹?    鈹?              鈹?     鈹?                 鈹?   鈹?                 鈹?    鈹?Newsletter-   鈹?     鈹?getEmailDesign() 鈹?   鈹?鐩存帴瀛樺偍 #ffffff 鈹?    鈹?Preview       鈹?     鈹?(鍙鐞?HEX)      鈹?   鈹?                 鈹?    鈹?(newsletter-  鈹?     鈹?                 鈹?   鈹?                 鈹?    鈹? preview.tsx) 鈹?     鈹?                 鈹?   鈹?                 鈹?    鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?     鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?```

#### Newsletter 妯″瀷榛樿鍊?
**鏂囦欢**锛歚ghost/core/core/server/models/newsletter.js:9-41`

```javascript
defaults: function defaults() {
    return {
        // 璇箟鍊硷紙闈?HEX锛?        background_color: 'light',           // newsletter.js:30
        header_background_color: 'transparent',  // newsletter.js:32
        button_color: 'accent',              // newsletter.js:28
        link_color: 'accent',                // newsletter.js:29
        
        // 鏋氫妇鍊?        button_corners: 'rounded',           // newsletter.js:36
        button_style: 'fill',                // newsletter.js:34
        title_font_weight: 'bold',           // newsletter.js:22
        link_style: 'underline',             // newsletter.js:31
        image_corners: 'square',             // newsletter.js:37
        
        // 甯冨皵鍊?        show_badge: true,
        show_header_icon: true,
        show_header_title: true,
        show_feature_image: true,
        show_comment_cta: true,
        feedback_enabled: false
    };
}
```

#### 绠＄悊绔瑙堣В鏋?
**鏂囦欢**锛歚apps/admin-x-settings/src/components/settings/email/newsletters/newsletter-preview.tsx:26-52`

```typescript
// 绠＄悊绔嫭绔嬪疄鐜拌涔夊€?鈫?HEX 鐨勮浆鎹?const backgroundColor = () => {
    const value = newsletter.background_color;
    const validHex = /#([0-9a-f]{3}){1,2}$/i;
    
    if (validHex.test(value)) {
        return value;
    }
    return '#ffffff';           // 'light' 鎴栧叾浠栨棤鏁堝€?鈫?鐧借壊
};

const headerBackgroundColor = () => {
    const value = newsletter.header_background_color;
    
    if (!value || value === 'transparent') {
        return 'transparent';
    }
    const validHex = /#([0-9a-f]{3}){1,2}$/i;
    if (validHex.test(value)) {
        return value;
    }
    return 'transparent';
};

const buttonColor = () => {
    const value = newsletter.button_color;
    const validHex = /#([0-9a-f]{3}){1,2}$/i;
    
    if (validHex.test(value || '')) {
        return value;
    }
    
    if (value === null) {
        const bg = backgroundColor();
        return textColorForBackgroundColor(bg).hex();
    }
    
    return siteData.accent_color;  // 'accent' 鎴栧叾浠栨棤鏁堝€?鈫?绔欑偣涓昏壊
};

const linkColor = () => {
    const value = newsletter.link_color === undefined ? 'accent' : newsletter.link_color;
    
    if (value === 'accent') {
        return siteData.accent_color;
    }
    
    const validHex = /#([0-9a-f]{3}){1,2}$/i;
    if (validHex.test(value || '')) {
        return value;
    }
    
    return textColorForBackgroundColor(backgroundColor()).hex();
};
```

#### 绠＄悊绔璁″伐鍏峰嚱鏁?
**鏂囦欢**锛歚apps/admin-x-settings/src/components/settings/email-design/design-utils.ts:18-128`

```typescript
const VALID_HEX = /^#(?:[0-9a-f]{3}){1,2}$/i;

function resolveBackgroundColor(value: string): string {
    if (VALID_HEX.test(value)) return value;
    return '#ffffff';
}

function resolveHeaderBackgroundColor(value: string, accentColor: string): string {
    if (!value || value === 'transparent') return 'transparent';
    if (value === 'accent') return accentColor;
    if (VALID_HEX.test(value)) return value;
    return 'transparent';
}

function resolveButtonColor(value: string | null, accentColor: string, bgColor: string): string {
    if (VALID_HEX.test(value || '')) return value!;
    if (value === null) return textColorForBackgroundColor(bgColor).hex();
    return accentColor;
}

export function resolveAllColors(settings: EmailDesignSettings, accentColor: string): ResolvedEmailColors {
    const bgColor = resolveBackgroundColor(settings.background_color);
    const headerBgColor = resolveHeaderBackgroundColor(settings.header_background_color, accentColor);
    const buttonColor = resolveButtonColor(settings.button_color, accentColor, bgColor);
}

export function resolveButtonCorners(corners: string | undefined): string {
    switch (corners) {
    case 'square': return 'rounded-none';
    case 'pill': return 'rounded-full';
    case 'rounded':
    default: return 'rounded-[6px]';
    }
}
```

#### 閭欢娓叉煋绔?getEmailDesign

**鏂囦欢**锛歚ghost/core/core/server/services/email-rendering/email-design.js:71-198`

```javascript
// email-design.js 鍙鐞?HEX 棰滆壊锛屼笉璇嗗埆 'light'銆?transparent' 绛夎涔夊€?
const DEFAULT_ACCENT_COLOR = '#15212A';     // email-design.js:29
const DEFAULT_DIVIDER_COLOR = '#e0e7eb';    // email-design.js:30
const VALID_HEX_REGEX = /^#([0-9a-f]{3}){1,2}$/i;  // email-design.js:27

const getHexColor = (value, fallback) => (isValidHexColor(value) ? value : fallback);

exports.getEmailDesign = (settings) => {
    // accentColor: 鏃犳晥鍊煎洖閫€鍒?DEFAULT_ACCENT_COLOR (#15212A)
    const accentColor = getHexColor(settings.accentColor, DEFAULT_ACCENT_COLOR);  // email-design.js:76
    
    // backgroundColor: 鏃犳晥鍊煎洖閫€鍒?#ffffff锛?light' 琚涓烘棤鏁堬級
    const backgroundColor = getHexColor(settings.backgroundColor, '#ffffff');      // email-design.js:79
    
    // buttonColor: 'accent' 琚壒娈婂鐞嗭紝鍏朵粬鏃犳晥鍊煎洖閫€鍒?accentColor
    let buttonColor;
    switch (settings.buttonColor) {
    case 'accent':
        buttonColor = accentColor;                                    // email-design.js:84
        break;
    case null:
        buttonColor = textColorForBackgroundColor(backgroundColor).hex();  // email-design.js:87
        break;
    default:
        buttonColor = getHexColor(settings.buttonColor, accentColor);     // email-design.js:91
        break;
    }
    
    // dividerColor: 'accent' 琚壒娈婂鐞?    let dividerColor;
    if (settings.dividerColor === 'accent') {
        dividerColor = accentColor;                                 // email-design.js:96
    } else {
        dividerColor = getHexColor(settings.dividerColor, DEFAULT_DIVIDER_COLOR);  // email-design.js:99
    }
    
    // headerBackgroundColor: 'accent' 琚壒娈婂鐞嗭紝鍏朵粬鏃犳晥鍊?鈫?null
    let headerBackgroundColor;
    if (settings.headerBackgroundColor === 'accent') {
        headerBackgroundColor = accentColor;                        // email-design.js:104
    } else if (isValidHexColor(settings.headerBackgroundColor)) {
        headerBackgroundColor = settings.headerBackgroundColor;     // email-design.js:107
    } else {
        headerBackgroundColor = null;  // 'transparent' 鈫?null      // email-design.js:110
    }
    
    // linkColor: 'accent' 琚壒娈婂鐞?    let linkColor;
    switch (settings.linkColor) {
    case 'accent':
        linkColor = accentColor;                                    // email-design.js:116
        break;
    case null:
        linkColor = textColorForBackgroundColor(backgroundColor).hex();  // email-design.js:119
        break;
    default:
        linkColor = getHexColor(settings.linkColor, accentColor);       // email-design.js:123
        break;
    }
    
    // titleWeight: 鏋氫妇鍊兼槧灏?    let titleWeight;
    switch (settings.titleFontWeight) {
    case 'normal': titleWeight = 400; break;
    case 'medium': titleWeight = 500; break;
    case 'semibold': titleWeight = 600; break;
    default: titleWeight = 700; break;
    }
    
    return {
        accentColor,
        backgroundColor,
        backgroundIsDark: isDark(backgroundColor),
        buttonColor,
        buttonTextColor: textColorForBackgroundColor(buttonColor).hex(),
        headerBackgroundColor,
        linkColor,
        titleWeight: String(titleWeight)
    };
};
```

#### 鍗曞厓娴嬭瘯楠岃瘉璇箟鍊煎鐞?
**鏂囦欢**锛歚ghost/core/test/unit/server/services/email-rendering/email-design.test.js:11-32`

```javascript
const INVALID_HEX_COLORS = [
    null,
    undefined,
    0xabcdef,
    ['#ff9900'],
    '',
    'invalid',
    'accent',       // email-design.test.js:18
    'light',        // email-design.test.js:19
    'dark',         // email-design.test.js:20
    '#',
    '#F'
];

it('returns the default background color when input is invalid', function () {
    for (const backgroundColor of INVALID_HEX_COLORS) {
        const result = getEmailDesign({...baseSettings, backgroundColor});
        assertColorsEqual(result.backgroundColor, '#ffffff');  // 閮藉洖閫€鍒扮櫧鑹?    }
});
```

#### EmailRenderer 璋冪敤 getEmailDesign

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:1025-1058`

```javascript
async getTemplateData({post, newsletter, html, addPaywall, segment}) {
    const emailDesign = this.#getEmailDesign(newsletter);              // email-renderer.js:1026
    
    const data = {
        // CSS 鍙橀噺锛氬睍寮€ emailDesign
        ...emailDesign,                                               // email-renderer.js:1047
        // ...
    };
}

#getEmailDesign(newsletter) {
    return getEmailDesign({
        accentColor: this.#settingsCache?.get('accent_color'),
        backgroundColor: newsletter?.get('background_color'),         // 'light'
        buttonColor: newsletter?.get('button_color'),                 // 'accent'
        buttonCorners: newsletter?.get('button_corners'),             // 'rounded'
        buttonStyle: newsletter?.get('button_style'),                 // 'fill'
        headerBackgroundColor: newsletter?.get('header_background_color'),  // 'transparent'
        linkColor: newsletter?.get('link_color'),                     // 'accent'
        linkStyle: newsletter?.get('link_style'),                     // 'underline'
        titleFontWeight: newsletter?.get('title_font_weight')         // 'bold'
    });
}
```

#### 璇箟鍊?鈫?HEX 杞崲璺緞

| 瀛楁 | 鏁版嵁搴撳€?| email-design.js 澶勭悊 | 鏈€缁?HEX |
|------|----------|---------------------|----------|
| `background_color` | `'light'` | `getHexColor('light', '#ffffff')` 鈫?鏃犳晥 | `#ffffff` |
| `header_background_color` | `'transparent'` | `isValidHexColor('transparent')` 鈫?鍚?| `null` |
| `button_color` | `'accent'` | `case 'accent'` 鈫?`accentColor` | 绔欑偣 `accent_color` |
| `link_color` | `'accent'` | `case 'accent'` 鈫?`accentColor` | 绔欑偣 `accent_color` |

#### Handlebars 妯℃澘娉ㄥ叆鏍峰紡

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-templates/partials/styles.hbs:9-16`

```handlebars
/* emailDesign 灞曞紑鍚庢敞鍏?*/
.header {
    {{#if headerBackgroundColor}}
    background-color: {{headerBackgroundColor}};
    {{else}}
    background-color: {{backgroundColor}};
    {{/if}}
}
```

---

## 涓€у寲鍙橀噺缁戝畾鍒颁細鍛樺瓧娈?
### 鍙橀噺璇硶

閭欢妯℃澘浣跨敤 `%%{variable}%%` 璇硶鏍囪瘑涓€у寲鍙橀噺锛?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-templates/template.hbs`

```handlebars
<a href="%%{unsubscribe_url}%%">{{t 'Unsubscribe'}}</a>
<p class="%%{name_class}%%">{{t 'Name'}}: %%{name, "not provided"}%%</p>
<p>{{t 'Email'}}: <a href="#">%%{email}%%</a></p>
```

### 鏀寔鐨勫彉閲忓畾涔?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:719-810`

```javascript
buildReplacementDefinitions({html, newsletterUuid}) {
    const t = this.#t;           // 鐪熷疄鐨?i18n.t 瀹炰緥
    const locale = this.#getValidLocale();

    const baseDefinitions = [
        {
            id: 'unsubscribe_url',
            getValue: (member) => {
                return this.createUnsubscribeUrl(member.uuid, {newsletterUuid});
            }
        },
        {
            id: 'manage_account_url',
            getValue: () => this.createManageAccountUrl()
        },
        {
            id: 'uuid',
            getValue: (member) => member.uuid
        },
        {
            id: 'key',  // HMAC 绛惧悕
            getValue: (member) => {
                return crypto.createHmac('sha256', this.#settingsHelpers.getMembersValidationKey())
                    .update(member.uuid).digest('hex');
            }
        },
        {
            id: 'first_name',
            getValue: (member) => member.name?.split(' ')[0]
        },
        {
            id: 'name',
            getValue: (member) => member.name
        },
        {
            id: 'name_class',  // name 涓虹┖鏃惰繑鍥?'hidden'
            getValue: (member) => member.name ? '' : 'hidden'
        },
        {
            id: 'email',
            getValue: (member) => member.email
        },
        {
            id: 'created_at',
            getValue: (member) => {
                const timezone = this.#settingsCache.get('timezone');
                return member.createdAt ? formatDateLong(member.createdAt, timezone, locale) : '';
            }
        },
        {
            id: 'status',
            getValue: (member) => {
                if (member.status === 'comped') {
                    return t('complimentary');
                }
                if (this.isMemberTrialing(member)) {
                    return t('trialing');
                }
                return t(member.status);
            }
        },
        {
            id: 'status_text',
            getValue: (member) => this.getMemberStatusText(member)
        },
        {
            id: 'list_unsubscribe',  // 閭欢澶?List-Unsubscribe
            getValue: (member) => {
                return this.createUnsubscribeUrl(member.uuid, {newsletterUuid});
            },
            required: true
        },
        {
            id: 'uniqueid',  // 缁曡繃 ESP 鍥剧墖浠ｇ悊缂撳瓨
            getValue: () => crypto.randomUUID()
        }
    ];
```

### Fallback 鏈哄埗

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:813-860`

```javascript
const EMAIL_REPLACEMENT_REGEX = /%%\{(.*?)\}%%/g;
const REPLACEMENT_STRING_REGEX = /^(?<recipientProperty>\w+?)(?:,? *(?:"|&quot;)(?<fallback>.*?)(?:"|&quot;))?$/;

const replacements = [];

// 鎵弿 HTML 涓疄闄呬娇鐢ㄧ殑鍙橀噺
let result;
while ((result = EMAIL_REPLACEMENT_REGEX.exec(html)) !== null) {
    const [replacementMatch, replacementStr] = result;
    
    // 鍘婚噸
    if (replacements.find(r => r.id === replacementStr)) {
        continue;
    }
    
    // 瑙ｆ瀽锛?%{name, "not provided"}%%
    const match = replacementStr.match(REPLACEMENT_STRING_REGEX);
    if (match) {
        const {recipientProperty, fallback} = match.groups;
        
        const definition = baseDefinitions.find(d => d.id === recipientProperty);
        if (definition) {
            replacements.push({
                id: replacementStr,
                originalId: recipientProperty,
                token: new RegExp(...),
                // 鏈?fallback 鏃讹細鍊间负绌哄垯杩斿洖 fallback
                getValue: fallback ? (member => definition.getValue(member) || fallback) : definition.getValue
            });
        }
    }
}
```

### 璁㈤槄鐘舵€佹枃鏈?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:642-709`

```javascript
getMemberStatusText(member) {
    const t = this.#t;
    const locale = this.#getValidLocale();

    if (member.status === 'free') {
        return t(messages.subscriptionStatus.free);
    }

    if (member.status === 'gift') {
        const expires = member.tiers[0]?.expiry_at ?? null;
        if (expires) {
            const timezone = this.#settingsCache.get('timezone');
            const date = formatDateLong(expires, timezone, locale);
            return t(messages.subscriptionStatus.giftExpires, {date});
        }
        return '';
    }

    if (member.status === 'paid') {
        let activeSubscription = member.subscriptions.find((subscription) => {
            return subscription.status === 'trialing' || subscription.status === 'active';
        });
        if (!activeSubscription) {
            return '';
        }
        
        if (activeSubscription.trial_end_at && activeSubscription.trial_end_at > new Date()) {
            const date = formatDateLong(activeSubscription.trial_end_at, timezone, locale);
            return t(messages.subscriptionStatus.trial, {date});
        }

        const date = formatDateLong(activeSubscription.current_period_end, timezone, locale);
        if (activeSubscription.cancel_at_period_end) {
            return t(messages.subscriptionStatus.canceled, {date});
        }
        return t(messages.subscriptionStatus.active, {date});
    }

    if (member.status === 'comped') {
        const expires = member.tiers[0]?.expiry_at ?? null;
        if (expires) {
            const timezone = this.#settingsCache.get('timezone');
            const date = formatDateLong(expires, timezone, locale);
            return t(messages.subscriptionStatus.complimentaryExpires, {date});
        }
        return t(messages.subscriptionStatus.complimentaryInfinite);
    }

    return '';
}
```

### 鏋勫缓鏃舵壂鎻?vs 鍙戦€佹椂缁戝畾

**姝ラ 1 - 鏋勫缓鏃?*锛歚EmailRenderer.buildReplacementDefinitions()` 鎵弿 HTML锛屽彧鏋勫缓瀹為檯浣跨敤鐨勫彉閲忓畾涔?
**姝ラ 2 - 鍙戦€佹椂**锛歚SendingService.buildRecipients()` 涓烘瘡涓細鍛樿绠楀疄闄呭€?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/sending-service.js:162-182`

```javascript
buildRecipients(members, replacementDefinitions) {
    return members.map((member) => {
        return {
            email: member.email?.trim(),
            replacements: replacementDefinitions.map((def) => {
                return {
                    id: def.id,
                    token: def.token,
                    value: def.getValue(member) || ''  // 姣忎釜浼氬憳鐙珛璁＄畻
                };
            })
        };
    }).filter((recipient) => {
        // Remove invalid recipient email addresses
        const isValidRecipient = validator.isEmail(recipient.email, {legacy: false});
        if (!isValidRecipient) {
            logging.warn(`Removed recipient ${recipient.email} from list because it is not a valid email address`);
        }
        return isValidRecipient;
    });
}
```

---

## 鎵归噺鍙戦€佷笌璺ㄩ偖绠卞鎴风鍏煎

### 瀹屾暣鍙戦€侀摼璺?
```
emailJob({emailId})                  batch-sending-service.js:133-190
  鈹?  鈹溾攢鈹€ updateStatusLock('submitting')           140-145
  鈹斺攢鈹€ sendEmail(email)                         161
       鈹?       鈹溾攢鈹€ getLazyRelation('newsletter')       201-203
       鈹溾攢鈹€ getLazyRelation('post')             205-207
       鈹溾攢鈹€ getBatches(email)                   209-211
       鈹?    鈹斺攢鈹€ EmailBatch.findAll()          228
       鈹?       鈹溾攢鈹€ createBatches(...)                  214 (濡傛灉娌℃湁鎵规)
       鈹?    鈹斺攢鈹€ (瑙佷笅鏂?
       鈹?       鈹斺攢鈹€ sendBatches(...)                    216
            鈹斺攢鈹€ (瑙佷笅鏂?
```

### 鍒嗘壒绛栫暐

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:237-340`

```javascript
async createBatches({email, post, newsletter}) {
    logging.info(`Creating batches for email ${email.id}`);

    // 1. 鍩熷悕棰勭儹闄愬埗
    let domainWarmupLimit = Infinity;
    if (this.#domainWarmingService.isEnabled()) {
        domainWarmupLimit = Number.isInteger(email.get('csd_email_count')) 
            ? email.get('csd_email_count') 
            : Infinity;  // batch-sending-service.js:241-244
    }

    // 2. 鑾峰彇鍒嗘锛堝彲鑳芥槸 [null] 鎴?['status:free', 'status:-free']锛?    const segments = await this.#emailRenderer.getSegments(post);  // 246
    const batches = [];
    const BATCH_SIZE = this.#sendingService.getMaximumRecipients();  // 248
    let totalCount = 0;

    for (const segment of segments) {
        logging.info(`Creating batches for email ${email.id} segment ${segment}`);

        // 3. 鏋勫缓 NQL filter锛坣ewsletter + recipient_filter + segment锛?        const segmentFilter = this.#emailSegmenter.getMemberFilterForSegment(
            newsletter, 
            email.get('recipient_filter'), 
            segment
        );  // 254

        // 4. ObjectId 娓告爣鍒嗛〉
        // 娉細浣跨敤 id 鑰岄潪 created_at锛屽洜涓哄鍏ヤ細鍛樺彲鑳借缃?created_at 涓鸿繃鍘?鏈潵鍊?        let lastId = email.id;  // 261

        while (!members || lastId) {
            logging.info(`Fetching members batch for email ${email.id} segment ${segment}, lastId: ${lastId}`);

            // 5. 鏌ヨ锛歩d < email.id + 鍊掑簭 + 鍒嗛〉
            const filter = segmentFilter + `+id:<'${lastId}'`;  // 266

            members = await this.#models.Member.getFilteredCollectionQuery({filter})
                .orderByRaw('id DESC')
                .select('members.id', 'members.uuid', 'members.email', 'members.name')
                .limit(BATCH_SIZE + 1);  // 269-271

            if (members.length > 0) {
                // 6. 鍩熷悕棰勭儹锛氬喅瀹氭槸鍚﹂渶瑕佹媶鍒嗘壒娆?                const remainingCustomDomainCapacity = domainWarmupLimit - totalCount;
                const membersToProcess = Math.min(members.length, BATCH_SIZE);

                const shouldSplitBatch = remainingCustomDomainCapacity > 0 
                    && remainingCustomDomainCapacity < membersToProcess;  // 278

                if (shouldSplitBatch) {
                    // 鎷嗗垎鎵规锛氶儴鍒嗚蛋鑷畾涔夊煙鍚嶏紝閮ㄥ垎璧?fallback
                    totalCount += await this.#createBatchWithRetry({
                        email, segment,
                        members: members.slice(0, remainingCustomDomainCapacity),
                        useFallbackDomain: false,
                        batches
                    });  // 281-287

                    totalCount += await this.#createBatchWithRetry({
                        email, segment,
                        members: members.slice(remainingCustomDomainCapacity, membersToProcess),
                        useFallbackDomain: true,
                        batches
                    });  // 288-294
                } else {
                    // 鍗曚竴鎵规
                    totalCount += await this.#createBatchWithRetry({
                        email, segment,
                        members: members.slice(0, membersToProcess),
                        useFallbackDomain: totalCount >= domainWarmupLimit,
                        batches
                    });  // 297-303
                }
            }

            // 7. 娓告爣鏇存柊锛氬鏋滄湁 BATCH_SIZE + 1 鏉★紝璇存槑杩樻湁鏇村
            if (members.length > BATCH_SIZE) {
                lastId = members[members.length - 2].id;  // 308
            } else {
                break;  // 310
            }
        }
    }

    logging.info(`Created ${batches.length} batches for email ${email.id} with ${totalCount} recipients`);

    // 8. 鏍￠獙骞舵洿鏂?email_count锛堜笌鍒涘缓閭欢鏃剁殑浼扮畻鍊煎姣旓級
    if (email.get('email_count') !== totalCount) {
        logging.error(`Email ${email.id} has wrong stored email_count ${email.get('email_count')}, did expect ${totalCount}. Updating the model.`);

        // 璇樊鐜?> 1% 鏃惰褰曞埌 Sentry
        const errorRate = Math.abs((totalCount - email.get('email_count')) / email.get('email_count'));
        if (this.#sentry && errorRate >= 0.01) {
            this.#sentry.captureMessage(`Email ${email.id} has wrong stored email_count...`);
        }

        const newEmailUpdate = {
            email_count: totalCount
        };
        if (this.#domainWarmingService.isEnabled()) {
            newEmailUpdate.csd_email_count = Math.min(totalCount, domainWarmupLimit);
        }

        await email.save(newEmailUpdate, {patch: true, require: false, autoRefresh: false});  // 337
    }

    return batches;  // 339
}
```

### #createBatchWithRetry 鍖呰

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:352-370`

```javascript
async #createBatchWithRetry({email, segment, members, useFallbackDomain, batches}) {
    if (members.length === 0) {
        return 0;  // 354
    }

    const batch = await this.retryDb(
        async () => {
            return await this.createBatch(email, segment, members, {
                useFallbackDomain
            });  // 359
        },
        {
            ...this.#getBeforeRetryConfig(email),
            description: `createBatch email ${email.id} segment ${segment}${useFallbackDomain ? ' (fallback domain)' : ' (custom domain)'}`
        }
    );  // 357-367

    batches.push(batch);  // 368
    return members.length;  // 369
}
```

### 鍒涘缓 EmailBatch + EmailRecipient

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:382-426`

```javascript
async createBatch(email, segment, members, options) {
    // 1. 濡傛灉娌℃湁浜嬪姟锛屽垱寤烘柊浜嬪姟
    if (!options || !options.transacting) {
        return this.#models.EmailBatch.transaction(async (transacting) => {
            return this.createBatch(email, segment, members, {transacting, ...options});
        });  // 383-386
    }

    logging.info(`Creating batch for email ${email.id} segment ${segment} with ${members.length} members`);

    // 2. 鍒涘缓 EmailBatch
    const batch = await this.#models.EmailBatch.add({
        email_id: email.id,
        member_segment: segment,
        status: 'pending',
        fallback_sending_domain: Boolean(options.useFallbackDomain)
    }, options);  // 391-396

    // 3. 鏋勫缓 EmailRecipient 鏁版嵁
    const recipientData = [];

    members.forEach((memberRow) => {
        if (!memberRow.id || !memberRow.uuid || !memberRow.email) {
            logging.warn(`Member row not included as email recipient due to missing data - id: ${memberRow.id}, uuid: ${memberRow.uuid}, email: ${memberRow.email}`);
            return;  // 401-404
        }

        recipientData.push({
            id: ObjectID().toHexString(),
            email_id: email.id,
            member_id: memberRow.id,
            batch_id: batch.id,
            member_uuid: memberRow.uuid,
            member_email: memberRow.email,
            member_name: memberRow.name
        });  // 406-414
    });

    // 4. 鎵归噺鎻掑叆 EmailRecipient
    const insertQuery = this.#db.knex('email_recipients').insert(recipientData);  // 417

    if (options.transacting) {
        insertQuery.transacting(options.transacting);  // 419-420
    }

    logging.info(`Inserting ${recipientData.length} recipients for email ${email.id} batch ${batch.id}`);
    await insertQuery;  // 424

    return batch;  // 425
}
```

### 鍒嗘娓叉煋妫€娴?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:335-361`

```javascript
async getSegments(post) {
    const allowedSegments = ['status:free', 'status:-free'];
    const html = await this.renderPostBaseHtml(post);

    // 1. 鏈?paywall card (<!--members-only-->) 鈫?蹇呴』鍒嗕袱娈?    if (html.indexOf('<!--members-only-->') !== -1) {
        return allowedSegments;  // 344
    }

    const $ = cheerioLoad(html);

    // 2. 妫€鏌?data-gh-segment 灞炴€?    let allSegments = $('[data-gh-segment]')
        .get()
        .map(el => el.attribs['data-gh-segment']);

    const segments = [...new Set(allSegments)].filter(segment => allowedSegments.includes(segment));
    
    if (segments.length === 0) {
        // 鏃犲樊寮?鈫?鍗曚竴娈?[null]
        return [null];  // 356
    }

    // 鏈夊樊寮?鈫?鍒嗕袱娈?    return allowedSegments;  // 375
}
```

### 搴旂敤鍒嗘杩囨护 + 瀹屾暣 renderBody 娴佺▼

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:393-585`

```javascript
async renderBody(post, newsletter, segment, options) {
    let html = await this.renderPostBaseHtml(post, newsletter);  // 394

    // 鈹€鈹€ 姝ラ 1锛歅aywall 鎴柇 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    const isPaidPost = post.get('visibility') === 'paid' || post.get('visibility') === 'tiers';  // 397
    const membersOnlyIndex = html.indexOf('<!--members-only-->');  // 398
    const hasMembersOnlyContent = membersOnlyIndex !== -1;  // 399
    let addPaywall = false;  // 400

    if (isPaidPost && hasMembersOnlyContent) {
        if (segment === 'status:free') {
            // 鍏嶈垂浼氬憳锛氭埅鏂唴瀹?+ 娣诲姞 paywall
            addPaywall = true;  // 405
            html = html.slice(0, membersOnlyIndex);  // 408
        }
    }

    let $ = cheerioLoad(html);  // 412

    // 鈹€鈹€ 姝ラ 2锛歞ata-gh-segment 杩囨护 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    // 蹇呴』鍦ㄦā鏉挎覆鏌撳墠鎵ц锛屽洜涓?preheader 鍙兘鐢ㄥ埌 HTML
    $('[data-gh-segment]').get().forEach((node) => {
        if (node.attribs['data-gh-segment'] !== segment) {
            $(node).remove();  // 421
        } else {
            $(node).removeAttr('data-gh-segment');  // 424
        }
    });  // 418-426

    html = $.html();  // 428

    // 鈹€鈹€ 姝ラ 3锛氭ā鏉挎覆鏌?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    const templateData = await this.getTemplateData({
        post, newsletter, html, addPaywall, segment
    });  // 430-436
    html = await this.renderTemplate(templateData);  // 437

    // 鈹€鈹€ 姝ラ 4锛氶摼鎺ヨ窡韪?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    const base = templateData.post.url;  // 440

    if (options.clickTrackingEnabled) {
        // 鏈夎窡韪細鏇挎崲涓鸿拷韪摼鎺?+ 娣诲姞 newsletter ref + post attribution
        html = await this.#linkReplacer.replace(html, async (url, originalPath) => {
            if (originalPath.startsWith('%%{') && originalPath.endsWith('}%%')) {
                return originalPath;  // 446-448
            }
            if (originalPath === '#') {
                return originalPath;  // 451-452
            }
            // 璺宠繃鍖呭惈鍏朵粬 replacement patterns 鐨勯摼鎺ワ紙%{key}%% 闇€瑕?Mailgun 鐩存帴鏇挎崲锛?            const urlStr = url.toString();
            const withoutUuid = urlStr.replace(/%%\{uuid\}%%/g, '');
            if (/%%\{[^}]+\}%%/.test(withoutUuid)) {
                return urlStr;  // 459-462
            }
            // 娣诲姞 newsletter source attribution
            const isSite = this.#urlUtils.isSiteUrl(url);
            if (isSite) {
                url = this.#outboundLinkTagger.addToUrl(url, newsletter);
                url = this.#memberAttributionService.addPostAttributionTracking(url, post);
            } else {
                url = this.#outboundLinkTagger.addToUrl(url);
            }  // 465-476
            // 璺宠繃 Powered by Ghost badge
            if (url.hostname === 'ghost.org' && url.pathname === '/' && url.searchParams.get('via') === 'pbg-newsletter') {
                return url.toString();  // 479-481
            }
            // 娣诲姞鐐瑰嚮璺熻釜
            url = await this.#linkTracking.service.addTrackingToUrl(url, post, '--uuid--');
            const str = url.toString().replace(/--uuid--/g, '%%{uuid}%%');
            return str;  // 484-488
        }, {base});
    } else {
        // 鏃犺窡韪細浠呮浛鎹㈢浉瀵归摼鎺ヤ负缁濆閾炬帴
        html = await this.#linkReplacer.replace(html, (url, originalPath) => {
            if (originalPath.startsWith('%%{') && originalPath.endsWith('}%%')) {
                return originalPath;  // 493-495
            }
            if (originalPath === '#') {
                return originalPath;  // 499-500
            }
            return url;  // 502
        }, {base});  // 490-504
    }

    // 鈹€鈹€ 姝ラ 5锛氳褰曞浘鐗囧師濮嬪昂瀵革紙Juice 鍓嶏級 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    // 濡傛灉 CSS 璁剧疆浜?width: auto 鎴?height: auto锛孞uice 浼氭樉寮忚缃埌灞炴€т笂
    // 杩欐槸 Outlook 涓嶆敮鎸佺殑锛岄渶瑕佹仮澶嶅師濮嬪€?    $ = cheerioLoad(html);  // 511
    const originalImageSizes = $('img').get().map((image) => {
        const src = image.attribs.src;
        const width = image.attribs.width;
        const height = image.attribs.height;
        return {src, width, height};
    });  // 512-517

    // 鈹€鈹€ 姝ラ 6锛氱粰 figcaption 鍔犵被锛圝uice 鍓嶏紝CSS 闇€瑕侊級 鈹€鈹€鈹€鈹€鈹€
    $('figcaption').each((i, elem) => !!($(elem).addClass('kg-card-figcaption')));  // 519-520
    html = $.html();  // 521

    // 鈹€鈹€ 姝ラ 7锛欽uice 鍐呰仈 CSS 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    const juice = require('juice');
    html = juice(html, {inlinePseudoElements: true, removeStyleTags: true});  // 523-525

    // 鈹€鈹€ 姝ラ 8锛欽uice 鍚庡鐞?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    $ = cheerioLoad(html);  // 528

    // 8a. 鎭㈠鍥剧墖 width/height = 'auto' 鈫?鍘熷鍊?    const imageTags = $('img').get();
    for (let i = 0; i < imageTags.length; i += 1) {
        if (imageTags[i].attribs.src === originalImageSizes[i].src) {
            if (imageTags[i].attribs.width === 'auto' && originalImageSizes[i].width) {
                imageTags[i].attribs.width = originalImageSizes[i].width;  // 536-537
            }
            if (imageTags[i].attribs.height === 'auto' && originalImageSizes[i].height) {
                imageTags[i].attribs.height = originalImageSizes[i].height;  // 539-540
            }
        }
    }  // 531-543

    // 8b. 寮哄埗鎵€鏈夐摼鎺ュ湪鏂扮獥鍙ｆ墦寮€
    $('a').attr('target', '_blank');  // 546

    // 8c. 璇箟鍖栨爣绛捐浆 div锛圤utlook margin 鏀寔闂锛?    $('figure, figcaption').each((i, elem) => !!(elem.tagName = 'div'));  // 549

    // 8d. 鏆楄壊/浜壊妯″紡鍥剧墖鍒囨崲锛圕SS 鏂规鍦?Outlook 涓嶇敓鏁堬級
    if (templateData.backgroundIsDark) {
        $('img.is-light-background').each((i, elem) => $(elem).remove());  // 553-555
    } else {
        $('img.is-dark-background').each((i, elem) => $(elem).remove());  // 557-559
    }  // 552-560

    // 鈹€鈹€ 姝ラ 9锛欻TML 鈫?鍙橀噺瀹氫箟 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    html = $.html();  // 563
    const replacementDefinitions = this.buildReplacementDefinitions({
        html, 
        newsletterUuid: newsletter.get('uuid')
    });  // 566

    // 鈹€鈹€ 姝ラ 10锛欻TML 鈫?绾枃鏈?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    const plaintext = htmlToPlaintext.email(html);  // 571

    // 鈹€鈹€ 姝ラ 11锛氱壒娈婂瓧绗﹁浆涔夛紙Outlook 鍏煎锛?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    html = html.replace(/&apos;/g, '&#39;');     // 鍗曞紩鍙?    html = html.replace(/鈫?g, '&rarr;');          // 鍙崇澶?    html = html.replace(/鈥?g, '&ndash;');         // 鐭牬鎶樺彿
    html = html.replace(/鈥?g, '&ldquo;');         // 宸﹀弻寮曞彿
    html = html.replace(/鈥?g, '&rdquo;');         // 鍙冲弻寮曞彿  // 574-578

    return {
        html,
        plaintext,
        replacements: replacementDefinitions
    };  // 580-584
}
```

### sendBatches 骞跺彂鎺у埗

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:428-479`

```javascript
async sendBatches({email, batches, post, newsletter}) {
    logging.info(`Sending ${batches.length} batches for email ${email.id}`);
    const deadline = this.getDeliveryDeadline(email);  // 430

    if (deadline) {
        logging.info(`Delivery deadline for email ${email.id} is ${deadline}`);
    }

    // 1. 娓叉煋缁撴灉缂撳瓨锛堟寜 segment 缂撳瓨锛屽悓涓€ segment 鐨勬壒娆″叡浜級
    /** @type {Map<string, import('./email-renderer').EmailBody>} */
    const emailBodyCache = new Map();  // 437

    // 2. 璁＄畻鎶曢€掓椂闂达紙濡傛灉鏈?deadline锛?    const deliveryTimes = this.calculateDeliveryTimes(email, batches.length);  // 440

    // 3. 闃熷垪 + 骞跺彂鎺у埗
    let succeededCount = 0;  // 443
    const queue = batches.slice();  // 444

    // 閫掑綊 worker
    let runNext;
    runNext = async () => {
        const batch = queue.shift();  // 449
        if (batch) {
            const batchData = {email, batch, post, newsletter, emailBodyCache, deliveryTime: undefined};
            // 鍙湁鍦ㄦ湁 deadline 涓旀湭杩囨湡鏃舵墠璁剧疆鎶曢€掓椂闂?            if (deadline && deadline.getTime() > Date.now()) {
                const deliveryTime = deliveryTimes.shift();
                if (deliveryTime && deliveryTime >= Date.now()) {
                    batchData.deliveryTime = deliveryTime;  // 456
                }
            }
            if (await this.sendBatch(batchData)) {
                succeededCount += 1;  // 460
            }
            await runNext();  // 462 - 閫掑綊璋冪敤
        }
    };  // 447-464

    // 鍚姩 MAX_SENDING_CONCURRENCY 涓苟鍙?worker
    await Promise.all(
        new Array(MAX_SENDING_CONCURRENCY).fill(0).map(() => runNext())
    );  // 467

    if (succeededCount < batches.length) {
        if (succeededCount > 0) {
            throw new errors.EmailError({
                message: tpl(messages.emailErrorPartialFailure)
            });
        }
        throw new errors.EmailError({
            message: tpl(messages.emailError)
        });  // 469-478
    }
}
```

### sendBatch 鍗曟壒娆″彂閫?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:486-601`

```javascript
async sendBatch({email, batch: originalBatch, post, newsletter, emailBodyCache, deliveryTime}) {
    logging.info(`Sending batch ${originalBatch.id} for email ${email.id}`);

    // 鈹€鈹€ 姝ラ 1锛氱姸鎬侀攣 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    const batch = await this.retryDb(
        async () => {
            return await this.updateStatusLock(
                this.#models.EmailBatch, 
                originalBatch.id, 
                'submitting', 
                ['pending', 'failed']
            );  // 493
        },
        {...this.#getBeforeRetryConfig(email), description: `updateStatusLock batch ${originalBatch.id} -> submitting`}
    );  // 491-496

    if (!batch) {
        logging.error(`Tried sending email batch that is not pending or failed ${originalBatch.id}`);
        return true;  // 499
    }

    let succeeded = false;  // 502

    try {
        // 鈹€鈹€ 姝ラ 2锛氳幏鍙栨壒娆′細鍛?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
        let members = await this.retryDb(
            async () => {
                const m = await this.getBatchMembers(batch.id);  // 507

                // 0 鏉¤褰?鈫?鍙兘鏄富浠庡鍒跺欢杩燂紝鎶涘嚭閲嶈瘯
                if (m.length === 0) {
                    throw new errors.EmailError({
                        message: `No members found for batch ${batch.id}, possible replication lag`
                    });  // 513
                }

                return m;  // 517
            },
            {...this.#getBeforeRetryConfig(email), description: `getBatchMembers batch ${originalBatch.id}`}
        );  // 505-520

        // 鈹€鈹€ 姝ラ 3锛氳皟鐢?SendingService.send 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
        const response = await this.retryDb(
            async () => {
                return await this.#sendingService.send({
                    emailId: email.id,
                    post,
                    newsletter,
                    segment: batch.get('member_segment'),
                    members
                }, {
                    openTrackingEnabled: !!email.get('track_opens'),
                    clickTrackingEnabled: !!email.get('track_clicks'),
                    useFallbackAddress: batch.get('fallback_sending_domain'),
                    deliveryTime,
                    emailBodyCache
                });  // 523-535
            }, 
            {...this.#MAILGUN_API_RETRY_CONFIG, description: `Sending email batch ${originalBatch.id} ...`}
        );  // 522-536

        succeeded = true;  // 537

        // 鈹€鈹€ 姝ラ 4锛氭洿鏂扮姸鎬佷负 submitted 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
        await this.retryDb(
            async () => {
                await batch.save({
                    status: 'submitted',
                    provider_id: response.id,
                    error_status_code: null,
                    error_message: null,
                    error_data: null
                }, {patch: true, require: false, autoRefresh: false});  // 541-548
            },
            {...this.#AFTER_RETRY_CONFIG, description: `save batch ${originalBatch.id} -> submitted`}
        );  // 539-551

    } catch (err) {
        // 鈹€鈹€ 閿欒澶勭悊 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
        if (err.code && err.code === 'BULK_EMAIL_SEND_FAILED') {
            logging.error(err);
            if (this.#sentry) {
                this.#sentry.captureException(err);
            }  // 553-558
        } else {
            const ghostError = new errors.EmailError({
                err,
                code: 'BULK_EMAIL_SEND_FAILED',
                message: `Error sending email batch ${batch.id}`,
                context: err.message
            });
            logging.error(ghostError);
            if (this.#sentry) {
                this.#sentry.captureException(err);
            }  // 560-571
        }

        if (!succeeded) {
            // 鍙湁鍦ㄧ‘瀹炴湭鍙戦€佹椂鎵嶆爣璁颁负 failed
            // 锛堢綍瑙佽竟鐣岋細宸插彂閫佷絾璁剧疆鐘舵€佸け璐ユ椂涓嶉噸鍙戯級
            await this.retryDb(
                async () => {
                    await batch.save({
                        status: 'failed',
                        error_status_code: err.statusCode ?? null,
                        error_message: err.message,
                        error_data: err.errorDetails ?? null
                    }, {patch: true, require: false, autoRefresh: false});  // 578-583
                },
                {...this.#AFTER_RETRY_CONFIG, description: `save batch ${originalBatch.id} -> failed`}
            );  // 576-586
        }
    }

    // 鈹€鈹€ 姝ラ 5锛氭爣璁版敹浠朵汉涓?processed 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
    await this.retryDb(
        async () => {
            await this.#models.EmailRecipient
                .where({batch_id: batch.id})
                .save({processed_at: new Date()}, {patch: true, require: false, autoRefresh: false});  // 593-595
        },
        {...this.#AFTER_RETRY_CONFIG, description: `save EmailRecipients ${originalBatch.id} processed_at`}
    );  // 591-598

    return succeeded;  // 600
}
```

### getBatchMembers锛欵mailRecipient 鈫?MemberLike 杞崲

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:609-635`

```javascript
async getBatchMembers(batchId) {
    // 1. 鏌ヨ EmailRecipient + 鍏宠仈 member銆乻tripeSubscriptions銆乸roducts
    let models = await this.#models.EmailRecipient.findAll({
        filter: `batch_id:'${batchId}'`, 
        withRelated: ['member', 'member.stripeSubscriptions', 'member.products']
    });  // 610

    // 2. 鏍￠獙鎵规澶у皬
    const BATCH_SIZE = this.#sendingService.getMaximumRecipients();
    if (models.length > BATCH_SIZE) {
        throw new errors.EmailError({
            message: `Email batch ${batchId} has ${models.length} members, which exceeds the maximum of ${BATCH_SIZE} members per batch.`
        });  // 614-616
    }

    // 3. 杞崲涓?MemberLike 鎺ュ彛锛堥殧绂诲疄鐜扮粏鑺傦級
    return models.map((model) => {
        const subscriptions = model.related('member').related('stripeSubscriptions').toJSON();
        const tiers = model.related('member').related('products').toJSON();

        return {
            id: model.get('member_id'),
            uuid: model.get('member_uuid'),
            email: model.get('member_email'),
            name: model.get('member_name'),
            createdAt: model.related('member')?.get('created_at') ?? null,
            status: model.related('member')?.get('status') ?? 'free',
            subscriptions,
            tiers
        };  // 624-633
    });
}
```

### updateStatusLock锛氳绾ч攣鐘舵€佹鏌?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:646-659`

```javascript
async updateStatusLock(Model, id, status, allowedStatuses) {
    let model;
    await Model.transaction(async (transacting) => {
        // 1. FOR UPDATE 琛岀骇閿?        model = await Model.findOne({id}, {require: true, transacting, forUpdate: true});  // 649

        // 2. 妫€鏌ュ綋鍓嶇姸鎬佹槸鍚﹀厑璁稿彉鏇?        if (!allowedStatuses.includes(model.get('status'))) {
            model = undefined;
            return;  // 651-653
        }

        // 3. 鏇存柊鐘舵€?        await model.save({status}, {patch: true, transacting, autoRefresh: false});  // 654-656
    });

    return model;  // 658
}
```

### retryDb锛氭寚鏁伴€€閬块噸璇?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:675-728`

```javascript
async retryDb(func, options) {
    // 1. 璁＄畻鎴鏃堕棿
    if (options.maxTime !== undefined) {
        const stopAfterDate = new Date(Date.now() + options.maxTime);
        if (!options.stopAfterDate || stopAfterDate < options.stopAfterDate) {
            options = {...options, stopAfterDate};  // 677-680
        }
    }

    const retryCount = (options.retryCount ?? 0);  // 682

    try {
        if (retryCount > 0) {
            logging.info(`[BULK_EMAIL_DB_RETRY] ${options.description} - Retrying ${retryCount + 1}th try`);
        } else {
            logging.info(`[BULK_EMAIL_DB_RETRY] ${options.description} - Started (1st try)`);
        }  // 685-689

        const response = await func();  // 691

        logging.info(`[BULK_EMAIL_DB_RETRY] ${options.description} - Finished (after ${retryCount + 1}${retryCount === 0 ? 'st try' : ' tries'})`);
        return response;  // 693-695

    } catch (e) {
        const sleep = (options.sleep ?? 0);  // 697

        // 2. 妫€鏌ユ槸鍚﹂渶瑕佺户缁噸璇?        if (retryCount >= options.maxRetries 
            || (options.stopAfterDate && (new Date(Date.now() + sleep)) > options.stopAfterDate)) {
            if (retryCount > 0) {
                const ghostError = new errors.EmailError({
                    err: e,
                    code: 'BULK_EMAIL_DB_RETRY',
                    message: `[BULK_EMAIL_DB_RETRY] ${options.description} - Failed and stopped retrying: ${retryCount >= options.maxRetries ? 'max retries reached' : 'max time reached'}`,
                    context: e.message
                });
                logging.error(ghostError);
            }
            throw e;  // 698-710
        }

        // 3. 璁板綍閿欒鏃ュ織
        const ghostError = new errors.EmailError({
            err,
            code: 'BULK_EMAIL_DB_RETRY',
            message: `[BULK_EMAIL_DB_RETRY] ${options.description} - Failed (${retryCount + 1}${retryCount === 0 ? 'st' : 'th'} try)`,
            context: e.message
        });
        logging.error(ghostError);  // 712-718

        // 4. 绛夊緟 + 鎸囨暟閫€閬匡紙sleep * 2锛?        if (sleep) {
            await new Promise((resolve) => setTimeout(resolve, sleep));
        }  // 721-724

        return await this.retryDb(
            func, 
            {...options, retryCount: retryCount + 1, sleep: sleep * 2}  // 鎸囨暟閫€閬?        );  // 726
    }
}
```

### #BEFORE/#AFTER/#MAILGUN 閲嶈瘯閰嶇疆

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:37-39`

```javascript
// 鍙戦€佸墠鐨勬暟鎹簱鎿嶄綔锛堢姸鎬佹鏌ャ€佹煡璇細鍛樼瓑锛?#BEFORE_RETRY_CONFIG = {maxRetries: 10, maxTime: 10 * 60 * 1000, sleep: 2000};     // 10娆￠噸璇曪紝鏈€澶?0鍒嗛挓

// 鍙戦€佸悗鐨勬暟鎹簱鎿嶄綔锛堟洿鏂扮姸鎬佺瓑锛?#AFTER_RETRY_CONFIG = {maxRetries: 20, maxTime: 30 * 60 * 1000, sleep: 2000};       // 20娆￠噸璇曪紝鏈€澶?0鍒嗛挓

// Mailgun API 璋冪敤
#MAILGUN_API_RETRY_CONFIG = {sleep: 10 * 1000, maxRetries: 6};                        // 6娆￠噸璇曪紝10绉掗棿闅?```

### #getBeforeRetryConfig锛氬姩鎬佹埅姝㈡椂闂?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/batch-sending-service.js:151-158`

```javascript
// 鍦?emailJob 涓绠椾弗鏍兼埅姝㈡椂闂?const expectedBatchCount = Math.ceil(email.get('email_count') / 1000);  // 152
const minimumSecondsPerBatch = 26;  // 153
const stopAfter = Math.max(
    expectedBatchCount * minimumSecondsPerBatch * 1000, 
    this.#BEFORE_RETRY_CONFIG.maxTime
);  // 154
const retryCutOffTime = new Date(startTime + stopAfter);  // 155

email._retryCutOffTime = retryCutOffTime;  // 158
```

### SendingService.send锛氱紦瀛?+ 璋冪敤 Provider

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/sending-service.js:110-154`

```javascript
async send({post, newsletter, segment, members, emailId}, options) {
    const cacheId = emailId + '-' + (segment ?? 'null');  // 111
    const isTestEmail = options.isTestEmail ?? false;  // 112

    /** @type {EmailBody | undefined} */
    let emailBody;

    // 1. 灏濊瘯浠庣紦瀛樿幏鍙?    if (options.emailBodyCache) {
        emailBody = options.emailBodyCache.get(cacheId);  // 120
    }

    // 2. 缂撳瓨鏈懡涓?鈫?娓叉煋
    if (!emailBody) {
        emailBody = await this.#emailRenderer.renderBody(
            post,
            newsletter,
            segment,
            {
                clickTrackingEnabled: !!options.clickTrackingEnabled
            }
        );  // 124-131
        
        if (options.emailBodyCache) {
            options.emailBodyCache.set(cacheId, emailBody);  // 133
        }
    }

    // 3. 鏋勫缓鏀朵欢浜?    const recipients = this.buildRecipients(members, emailBody.replacements);  // 137

    // 4. 璋冪敤 Provider
    return await this.#emailProvider.send({
        subject: this.#emailRenderer.getSubject(post, isTestEmail),
        from: this.#emailRenderer.getFromAddress(post, newsletter, !!options.useFallbackAddress),
        replyTo: this.#emailRenderer.getReplyToAddress(post, newsletter, !!options.useFallbackAddress) ?? undefined,
        html: emailBody.html,
        plaintext: emailBody.plaintext,
        recipients,
        emailId: emailId,
        replacementDefinitions: emailBody.replacements,
        domainOverride: options.useFallbackAddress ? this.#emailAddressService.fallbackDomain : undefined
    }, {
        clickTrackingEnabled: !!options.clickTrackingEnabled,
        openTrackingEnabled: !!options.openTrackingEnabled,
        useFallbackAddress: !!options.useFallbackAddress,
        ...(options.deliveryTime && {deliveryTime: options.deliveryTime})
    });  // 138-153
}
```

### 璺ㄩ偖绠卞鎴风鍏煎鎬у鐞嗘眹鎬?
| 姝ラ | 鍔熻兘 | 鏂囦欢 | 绮剧‘琛屽彿 | 璇佹嵁 |
|------|------|------|----------|------|
| 0. CSS 鏂规锛堣 HTML 鏂规瑕嗙洊锛?| 鏆楄壊鍥剧墖闅愯棌 | `styles.hbs` | 4-7 | `img.is-dark-background { display: none !important; mso-hide: all !important; }` 浣?Outlook 涓嶇敓鏁?|
| 1. figcaption 鍔犵被 | 涓?CSS 鏍峰紡鍑嗗 | `email-renderer.js` | 519-520 | `$('figcaption').addClass('kg-card-figcaption')` |
| 2. Juice 鍐呰仈 CSS | style 鏍囩 鈫?鍐呰仈灞炴€?| `email-renderer.js` | 523-525 | `juice(html, {inlinePseudoElements: true, removeStyleTags: true})` |
| 3. 鍥剧墖 auto 淇 | Outlook 涓嶆敮鎸?width: auto | `email-renderer.js` | 531-543 | `if (imageTags[i].attribs.width === 'auto') 鈫?鎭㈠ original` |
| 4. 寮哄埗鏂扮獥鍙?| 閾炬帴 target | `email-renderer.js` | 546 | `$('a').attr('target', '_blank')` |
| 5. 璇箟鍖栨爣绛捐浆 div | Outlook margin 闂 | `email-renderer.js` | 549 | `$('figure, figcaption').each(... tagName = 'div')` |
| 6. 鏆楄壊/浜壊鍥剧墖鍒囨崲 | 鏇夸唬 CSS 鏂规 | `email-renderer.js` | 552-560 | `$('img.is-light-background').remove()` 鍜?`$('img.is-dark-background').remove()` |
| 7. 鐗规畩瀛楃杞箟 | Outlook 鏄剧ず闂 | `email-renderer.js` | 574-578 | `replace(/&apos;/g, '&#39;')` 绛?5 涓浛鎹?|
| 8. PixelsPerInch | Outlook DPI 缂╂斁 | `email-wrapper.hbs` | 6 | `<!--[if mso]><o:PixelsPerInch>96</o:PixelsPerInch>` |
| 9. Header 灞呬腑 | Outlook 涓嶅皧閲?max-width | `email-wrapper.hbs` | 21-26 | `<!--[if mso]><center><table width="600">` |
| 10. Body 灞呬腑 | Outlook 涓嶅皧閲?max-width | `email-wrapper.hbs` | 168-173 | `<!--[if mso]><center><table width="600">` |

### Outlook 鏉′欢娉ㄩ噴瀹屾暣浣嶇疆

**鏂囦欢**锛歚ghost/core/core/server/services/email-rendering/partials/email-wrapper.hbs:6, 21-26, 156-161, 168-173, 187-192`

```handlebars
<!-- 浣嶇疆 1锛欴PI 缂╂斁锛堢 6 琛岋級 -->
<!--[if mso]><xml><o:OfficeDocumentSettings><o:PixelsPerInch>96</o:PixelsPerInch><o:AllowPNG/></o:OfficeDocumentSettings></xml><![endif]-->

<!-- 浣嶇疆 2锛欻eader 灞呬腑寮€濮嬶紙绗?21-26 琛岋級 -->
<!-- Outlook doesn't respect max-width so we need an extra centered table -->
<!--[if mso]>
<tr>
    <td>
        <center>
            <table border="0" cellpadding="0" cellspacing="0" width="600">
<![endif]-->

<!-- ... header 鍐呭 ... -->

<!-- Header 灞呬腑闂悎锛堢 156-161 琛岋級 -->
<!--[if mso]>
            </table>
        </center>
    </td>
</tr>
<![endif]-->

<!-- 浣嶇疆 3锛欱ody 灞呬腑寮€濮嬶紙绗?168-173 琛岋級 -->
<!-- Outlook doesn't respect max-width so we need an extra centered table -->
<!--[if mso]>
<tr>
    <td>
        <center>
            <table border="0" cellpadding="0" cellspacing="0" width="600">
<![endif]-->

<!-- ... body 鍐呭 ... -->

<!-- Body 灞呬腑闂悎锛堢 187-192 琛岋級 -->
<!--[if mso]>
            </table>
        </center>
    </td>
</tr>
<![endif]-->
```

---

## 澶氳瑷€鍏滃簳鏈哄埗

### 瀹屾暣 locale 澶勭悊閾捐矾

```
鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?鈹?                   settings_cache 琛紙鏁版嵁搴擄級                       鈹?鈹? key: 'locale', value: 'en'  (鐢ㄦ埛閰嶇疆锛岀畝鍐?                        鈹?鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?                                    鈹?            鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹尖攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?            鈻?                      鈻?                      鈻?    鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹屸攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?    鈹? email-service  鈹?   鈹?  EmailRenderer 鈹?   鈹?  鏃ユ湡鏍煎紡鍖?    鈹?    鈹? -wrapper.js    鈹?   鈹?                鈹?   鈹?                鈹?    鈹?                鈹?   鈹?#getValidLocale 鈹?   鈹?formatDateLong() 鈹?    鈹?i18nLib('en')   鈹?   鈹?'en' 鈫?'en-gb'  鈹?   鈹?Luxon setLocale 鈹?    鈹?(i18n 瀹炰緥)     鈹?   鈹?                鈹?   鈹?                鈹?    鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?   鈹斺攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹?```

### i18n 瀹炰緥鍒濆鍖?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-service-wrapper.js:62-95`

```javascript
// 浠?settings 璇诲彇 locale锛堝彲鑳芥槸 'en' 绠€鍐欙級
const i18nLanguage = settingsCache.get('locale') || 'en';  // email-service-wrapper.js:62

// 鍒涘缓 i18n 瀹炰緥
const i18n = i18nLib(i18nLanguage, 'ghost');  // email-service-wrapper.js:63

// 鐩戝惉 locale 鍙樺寲
events.on('settings.locale.edited', (model) => {
    debug('locale changed, updating i18n to', model.get('value'));
    i18n.changeLanguage(model.get('value'));  // email-service-wrapper.js:69
});

// 娉ㄥ叆 EmailRenderer
const emailRenderer = new EmailRenderer({
    settingsCache,
    settingsHelpers,
    t: i18n.t,           // email-service-wrapper.js:94
    dir: i18n.dir.bind(i18n)  // email-service-wrapper.js:95
});
```

### 鍗犱綅鍑芥暟 vs 鐪熷疄缈昏瘧鍑芥暟

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:26-48`

```javascript
/**
 * Wrapper function so that i18next-parser can find these strings.
 * 浠呯敤浜庨潤鎬佸垎鏋愭彁鍙栫炕璇?key锛屼笉鏄疄闄呰繍琛屾椂浣跨敤鐨勫嚱鏁帮紒
 */
const t = (x) => {
    return x;
};  // email-renderer.js:33

// 鐢ㄥ崰浣嶅嚱鏁版彁鍙栫炕璇?key锛坕18next-parser 闈欐€佸垎鏋愶級
const messages = {
    subscriptionStatus: {
        free: '',
        expired: t('Your subscription has expired.'),
        canceled: t('Your subscription has been canceled and will expire on {date}...'),
        active: t('Your subscription will renew on {date}.'),
        trial: t('Your free trial ends on {date}...'),
        complimentaryExpires: t('Your subscription will expire on {date}.'),
        complimentaryInfinite: '',
        giftExpires: t('Your subscription will expire on {date}.')
    }
};  // email-renderer.js:36-48
```

| 浣嶇疆 | 鍑芥暟 | 鐢ㄩ€?|
|------|------|------|
| 鏂囦欢椤堕儴 `const t = (x) => x` | 鍗犱綅鍑芥暟 | i18next-parser 闈欐€佸垎鏋愭彁鍙?key |
| 鏋勯€犲嚱鏁版敞鍏ョ殑 `this.#t` | `i18n.t` | 杩愯鏃跺疄闄呮墽琛岀炕璇?|

### locale 楠岃瘉涓庨檷绾?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:22, 66-74, 272-284`

```javascript
const DEFAULT_LOCALE = 'en-gb';  // email-renderer.js:22

function isValidLocale(locale) {
    try {
        // 浣跨敤 Intl.DateTimeFormat 楠岃瘉
        new Intl.DateTimeFormat(locale);
        return true;
    } catch (e) {
        return false;
    }
}  // email-renderer.js:66-74

#getValidLocale() {
    let locale = this.#settingsCache.get('locale') || DEFAULT_LOCALE;  // email-renderer.js:273
    // 鍙兘寰楀埌 'en'銆?zh' 绛夌畝鍐?    
    locale = locale.trim();  // email-renderer.js:276
    
    // 'en' 绠€鍐欎笉琚?Intl 瀹屽叏鏀寔锛屾垨 locale 鏃犳晥 鈫?闄嶇骇鍒?'en-gb'
    if (locale === 'en' || !isValidLocale(locale)) {
        locale = DEFAULT_LOCALE;  // email-renderer.js:280
    }
    
    return locale;
}  // email-renderer.js:272-284
```

### 鏃ユ湡鏈湴鍖?
**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:82-88`

```javascript
function formatDateLong(date, timezone, locale = DEFAULT_LOCALE) {
    return DateTime.fromJSDate(date)
        .setZone(timezone)
        .setLocale(locale)
        .toLocaleString({
            year: 'numeric',
            month: 'long',
            day: 'numeric'
        });
}
```

### RTL 鏂瑰悜鏀寔

**鏂囦欢**锛歚ghost/core/core/server/services/email-service/email-renderer.js:237-238, 1031, 1124`

```javascript
// 鏋勯€犲嚱鏁版敞鍏?constructor({
    settingsCache,
    settingsHelpers,
    t,
    dir  // email-renderer.js:237 - 娉ㄥ叆 i18n.dir
}) {
    this.#t = t;
    this.#dir = dir;  // email-renderer.js:238 - 璧嬪€?}

// 浣跨敤
async getTemplateData({post, newsletter, html, addPaywall, segment}) {
    const locale = this.#getValidLocale();  // email-renderer.js:1031
    const direction = this.#dir(locale);  // email-renderer.js:1124
    
    const data = {
        site: {
            locale,
            direction
        }
    };
}
```

### 澶氳瑷€鍏滃簳鎬荤粨

| 鍦烘櫙 | 澶勭悊鏂瑰紡 | 鏂囦欢 | 绮剧‘琛屽彿 |
|------|----------|------|----------|
| settings 瀛?`'en'` 绠€鍐?| `#getValidLocale()` 闄嶇骇 | `email-renderer.js` | 278-280 |
| locale 鏃犳晥 | `#getValidLocale()` 闄嶇骇 | `email-renderer.js` | 278-280 |
| 鏂囨湰缈昏瘧 | `i18n.t()` 鎵ц | `email-renderer.js` | 720, 778, 781 |
| 鏃ユ湡鏍煎紡 | Luxon `setLocale()` | `email-renderer.js` | 82-88 |
| 鏂囨湰鏂瑰悜 | `i18n.dir()` | `email-renderer.js` | 1024 |
| HTML lang/dir | 妯℃澘娉ㄥ叆 | `email-wrapper.hbs` | 2 |
| RTL 甯冨眬璋冩暣 | 鏉′欢鏍峰紡 | `styles.hbs` | 瑙佹牱寮忔枃浠?|

---

## 鍏抽敭鏂囦欢绱㈠紩

### 鏍稿績鏈嶅姟

| 鏂囦欢 | 鑱岃矗 |
|------|------|
| `ghost/core/core/server/services/email-service/email-service-wrapper.js` | 渚濊禆娉ㄥ叆銆乮18n 鍒濆鍖栥€佷簨浠剁洃鍚?|
| `ghost/core/core/server/services/email-service/email-renderer.js` | 閭欢娓叉煋鏍稿績锛堟ā鏉裤€佸彉閲忋€佸吋瀹瑰鐞嗭級 |
| `ghost/core/core/server/services/email-service/sending-service.js` | 鍙戦€佸崗璋冿紙缂撳瓨銆佹敹浠朵汉鏋勫缓锛?|
| `ghost/core/core/server/services/email-service/batch-sending-service.js` | 鎵归噺璋冨害锛堝垎鎵广€佸苟鍙戙€侀噸璇曘€佸煙鍚嶉鐑級 |

### 璁捐璁＄畻

| 鏂囦欢 | 鑱岃矗 |
|------|------|
| `ghost/core/core/server/services/email-rendering/email-design.js` | 鍚庣 HEX 棰滆壊璁＄畻锛堜笉澶勭悊璇箟鍊硷級 |
| `ghost/core/core/server/models/newsletter.js` | Newsletter 妯″瀷 + 璇箟鍊奸粯璁ゅ€?|
| `apps/admin-x-settings/src/components/settings/email/newsletters/newsletter-preview.tsx` | 绠＄悊绔瑙堬紙璇箟鍊?鈫?HEX 杞崲锛?|
| `apps/admin-x-settings/src/components/settings/email-design/design-utils.ts` | 绠＄悊绔璁″伐鍏峰嚱鏁帮紙璇箟鍊?鈫?HEX 杞崲锛?|

### 妯℃澘鏂囦欢

| 鏂囦欢 | 鑱岃矗 |
|------|------|
| `ghost/core/core/server/services/email-rendering/partials/email-wrapper.hbs` | HTML 楠ㄦ灦 + Outlook 鏉′欢娉ㄩ噴 |
| `ghost/core/core/server/services/email-service/email-templates/template.hbs` | 鍐呭鍖哄煙 + 椤佃剼 |
| `ghost/core/core/server/services/email-templates/partials/styles.hbs` | 鍔ㄦ€佹牱寮忔敞鍏?|

### 娴嬭瘯鏂囦欢

| 鏂囦欢 | 鑱岃矗 |
|------|------|
| `ghost/core/test/unit/server/services/email-rendering/email-design.test.js` | 楠岃瘉璇箟鍊艰瑙嗕负鏃犳晥 HEX |
| `ghost/core/test/unit/server/services/email-service/batch-sending-service.test.js` | 鎵归噺鍙戦€佸崟鍏冩祴璇?|
| `ghost/core/test/integration/services/email-service/batch-sending.test.js` | 鎵归噺鍙戦€侀泦鎴愭祴璇?|

